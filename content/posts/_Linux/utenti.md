### cambiare utente
`su nome_utente`
`-l` permette di creare nuova shell che ha tutto quello che utente puo fare

usermod -aG sudo its
aggiungi utente al gruppo sudo
per permettere agli utenti didiventare sudo